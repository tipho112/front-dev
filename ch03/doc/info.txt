# * { margin:0; padding:0; }
기본값 설정

# ex01
margin: 10px auto 10px auto; => 중앙정렬을 하고싶을떄 left,right에 auto값을 지정해준다. 단, width가 기준이기 때문에 with가 지정된 상태에서 사용 가능

# ex02
closure : 

# ex03
max-height : content가 max-height 보다 크더라도 크기가 조절되지 않음(최대값 설정), 초과되는 content(overflow)는 나오지 않음
min-height : content가 min-height 보다 작더라도 크기가 조절되지 않음(최소값 설정)
height만 주면 overflow는 동작하지 않음

# ex04